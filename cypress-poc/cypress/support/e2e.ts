// Global setup for E2E specs
// Runs before every E2E spec file

// Prevent Cypress from failing on expected React errors in failure tests
// Individual specs can override this with cy.on('uncaught:exception', ...)
